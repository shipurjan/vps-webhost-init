---
import ThumbsUpKakashi from '@/assets/thumbs-up-kakashi.jpg'
import Layout from '@/layouts/Layout.astro'
import { Picture } from 'astro:assets'
---

<Layout
  title="__#TEMPLATE#:DOMAIN__"
  description="Your VPS is live with Caddy reverse proxy, automatic HTTPS, and Docker container management."
>
  <div class="space-y-8">
    <div class="space-y-4">
      <div class="flex flex-col justify-center items-center space-y-4 mb-8">
        <Picture
          src={ThumbsUpKakashi}
          formats={['avif', 'webp']}
          width={320}
          densities={[1, 2]}
          alt="Anime character showing thumbs up"
          loading="eager"
          fetchpriority="high"
        />

        <h1 class="text-4xl font-bold text-gray-900">
          __#TEMPLATE#:DOMAIN__ is live!
        </h1>
      </div>

      <p class="text-lg text-gray-600">
        Your VPS is running with <strong class="text-gray-900">Caddy</strong> (reverse
        proxy + auto HTTPS) <br />and <strong class="text-gray-900"
          >Dozzle</strong
        > (logs at <code class="px-2 py-1 bg-gray-100 rounded text-sm"
          >logs.__#TEMPLATE#:DOMAIN__</code
        >).
      </p>
      <p class="text-lg text-gray-600">
        This frontend is <strong class="text-gray-900">Astro</strong> with vanilla
        JS. <br />Swap it for anything you want in <code
          class="px-2 py-1 bg-gray-100 rounded text-sm"
          >~/__#TEMPLATE#:DOMAIN__/frontend</code
        >.
      </p>
    </div>

    <nav>
      <a
        href="/about"
        class="inline-flex items-center text-blue-600 hover:text-blue-700 font-medium transition-colors"
      >
        About →
      </a>
    </nav>

    <!-- Spider trap - invisible to users, bots will follow -->
    <a href="/honeypot" class="absolute -left-[9999px]" aria-hidden="true" tabindex="-1"></a>
  </div>
</Layout>
